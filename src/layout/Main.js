import React from "react";
import ThemeWrapper from "./Theme";
import MainLayout from "./MainLayout";

const Main = () => {
  return (
    <ThemeWrapper>
      <MainLayout />
    </ThemeWrapper>
  );
};

export default Main;
