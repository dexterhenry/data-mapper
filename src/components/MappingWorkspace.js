import { makeStyles, styled } from "@mui/styles";

const useStyles = makeStyles((theme) => ({
        rootWrapper: {
          width: "100%",
          height: 300,
          backgroundColor: theme.palette.primary.main,
        },
      }));
      

const MappingWorkspace = () => {
        return (
                <div>
                        <h3>MappingWorkspace</h3>
                </div>
        )
}

export default MappingWorkspace
