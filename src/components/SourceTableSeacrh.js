import { makeStyles } from "@mui/styles";

const useStyles = makeStyles((theme) => ({
  rootWrapper: {
    width: "100%",
    height: 10,
    backgroundColor: theme.palette.secondary.main,
  },
}));

const SourceTableSearch = () => {
  return <div>Search</div>;
};

export default SourceTableSearch;
